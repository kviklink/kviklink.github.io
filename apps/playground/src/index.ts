// Imports /////////////////////////////////////////////////////////////////////
import { None, Some } from 'ts-results';
import { inspect } from 'util'
import { Xbs, XbsBuilder, Folder, Bookmark } from 'xbs'

// Main ////////////////////////////////////////////////////////////////////////

async function main() {
    const xbs = await new XbsBuilder().setCredentials('', '').finish()
    const a = xbs.unwrap()
    await a.get()
}

////////////////////////////////////////////////////////////////////////////////
main();
////////////////////////////////////////////////////////////////////////////////
